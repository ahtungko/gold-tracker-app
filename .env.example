VITE_APP_TITLE="Todo App"
VITE_APP_LOGO="https://placehold.co/40x40/3b82f6/ffffff?text=T"
VITE_VAPID_PUBLIC_KEY=""

# Infrastructure (server-only)
PORT=3000

# Web Push configuration
VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
VAPID_SUBJECT="mailto:support@example.com"
# Optional custom path for persisted web push subscriptions
PUSH_SUBSCRIPTIONS_PATH="./storage/push-subscriptions.json"

# Generate VAPID keys with:
#   npx web-push generate-vapid-keys
# Copy the resulting public/private keys into VAPID_PUBLIC_KEY and VAPID_PRIVATE_KEY
